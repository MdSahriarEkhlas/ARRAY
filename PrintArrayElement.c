#include <stdio.h>
int main() {
    // Declaration & Initialization
    int arr[5] = {10, 20, 30, 40, 50};

    // Print all elements of the array
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}